"use client";
import { useRouter } from "next/navigation";
import { useState } from "react";
export function NewProjectForm(){const router=useRouter();const[name,setName]=useState("Untitled Project");const[file,setFile]=useState<File|null>(null);const[progress,setProgress]=useState(0);const[error,setError]=useState("");const[busy,setBusy]=useState(false);async function create(){if(!file)return;setBusy(true);setError("");try{const p=await fetch("/api/projects",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({name})});if(!p.ok)throw new Error("Could not create project");const{project}=await p.json();await new Promise<void>((resolve,reject)=>{const xhr=new XMLHttpRequest();xhr.open("POST",`/api/projects/${project.id}/upload`);xhr.setRequestHeader("Content-Type",file.type||"application/octet-stream");xhr.setRequestHeader("X-File-Name",encodeURIComponent(file.name));xhr.upload.onprogress=e=>{if(e.lengthComputable)setProgress(Math.round(e.loaded/e.total*100))};xhr.onload=()=>xhr.status>=200&&xhr.status<300?resolve():reject(new Error(JSON.parse(xhr.responseText||"{}").error||"Upload failed"));xhr.onerror=()=>reject(new Error("Upload failed"));xhr.send(file)});router.push(`/creative-circle/project/${project.id}`)}catch(e){setError(e instanceof Error?e.message:"Upload failed");setBusy(false)}}return <div className="cc-panel" style={{display:"grid",gap:18}}><div className="field"><label>Project name</label><input className="input" value={name} maxLength={120} onChange={e=>setName(e.target.value)}/></div><label className="upload-drop"><input style={{display:"none"}} type="file" accept="video/mp4,video/quicktime,video/x-m4v,video/webm,.mp4,.mov,.m4v,.webm" onChange={e=>setFile(e.target.files?.[0]??null)}/><span className="micro" style={{color:"var(--lime)"}}>{file?"VIDEO READY":"DROP / CHOOSE VIDEO"}</span><h2 style={{fontSize:32,margin:"12px 0 8px"}}>{file?.name??"MP4 · MOV · M4V · WEBM"}</h2><p className="muted">The server validates the media with FFprobe before attaching it to the project.</p></label>{busy&&<div><div className="progress"><i style={{width:`${progress}%`}}/></div><small className="muted">Uploading {progress}%</small></div>}{error&&<div className="error" role="alert">{error}</div>}<button className="btn primary" onClick={create} disabled={!file||busy}>{busy?"UPLOADING…":"CREATE PROJECT ↘"}</button></div>}
